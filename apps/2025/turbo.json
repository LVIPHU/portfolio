{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        ".next/**", "!.next/cache/**",
        ".contentlayer/**",
        "public/feed.xml", "public/search.json", "public/tags/**"
      ]
    }
  }
}
